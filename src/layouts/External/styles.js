export default ({ palette }) => {
  return {
    root: {
      flex: '1',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      backgroundColor: palette.background.paper
    }
  }
}
